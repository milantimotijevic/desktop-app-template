const electron = require('electron');
const ipcRenderer = electron.ipcRenderer;


// ipcRenderer.on('some:event', (event, checked) => {
    // ipcRenderer.send('some:other-event', {name: 'John Doe'});
// });
